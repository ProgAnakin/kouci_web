# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TS build info
*.tsbuildinfo
node_modules/.tmp

# Local dev-assist skills (installed via `npx skills add`) — tooling for the
# team, not part of the shipped product. Re-add with the commands in the README
# if needed. Remove these lines to version them instead.
.agents/
skills-lock.json
